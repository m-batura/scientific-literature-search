# Two-step scientific literature search by title and abstract embeddings

`similarity_search.py` - file conatins maing functions for similarity search. For the first time, run `save_titles_embeddings` with save path. 
`experiments.py` - file with experiments in paper
